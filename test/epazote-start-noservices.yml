config:
    smtp:
        username: username
        password: password
        server: smtp server
        port: 587
        tls: true
        headers:
            from: from@email
            to: team@email
            subject: "[name - exit - status]"
    scan:
        hours: 1
        paths:
            - /my/service/path
