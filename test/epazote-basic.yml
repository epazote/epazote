#config:
    #smtp:
        #username: username
        #password: password
        #server: smtp server
        #port: 587
        #headers:
            #from: from@email
            #to: team@email
            #subject: "[name - exit - status]"

services:
    my site:
        url: http://www.google.com
        expect:
            status: 200
