config:
    smtp:
        headers:
            from: from@email
            subject: >
                [name, exit]

services:
    service X:
        test: pgrep nginx
        if_not:
            notify: yes
