---
services:
  myip:
    url: http://www.ssh-vault.com
    every: 10s
    headers:
      User-Agent: sopas
    expect:
      status: 200
      ssl:
        hours: 72
      if_not:
        cmd: echo -n "myip.country down"
